name: "Missing Steps Template"
base: "ubuntu-22.04-server-lts"
description: "This template is missing build steps"

# No build_steps section

validation:
  - name: "Sample validation"
    command: "echo test"
    success: true