name: "Invalid Validation Template"
base: "ubuntu-22.04-server-lts"
description: "Template with invalid validation"

build_steps:
  - name: "System update"
    script: |
      echo "This is a sample build step"

validation:
  - name: "Invalid validation"
    command: "echo test"
    # No validation criteria (success, contains, or equals)