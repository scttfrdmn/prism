import { __rest } from "tslib";
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
import React, { useLayoutEffect, useState } from 'react';
import { useMergeRefs } from '@cloudscape-design/component-toolkit/internal';
import InternalInput from '../../../input/internal';
import { getBaseProps } from '../../base-component';
import { useFormFieldContext } from '../../context/form-field-context';
import { fireCancelableEvent, fireNonCancelableEvent } from '../../events';
import { processAttributes } from '../../utils/with-native-attributes';
import useMask from './use-mask';
import MaskFormat from './utils/mask-format';
const MaskedInput = React.forwardRef((_a, ref) => {
    var { value, onBlur, onChange, onKeyDown, mask, autofix = false, disableAutocompleteOnBlur = false, showUnmaskedValue = false, nativeInputAttributes } = _a, rest = __rest(_a, ["value", "onBlur", "onChange", "onKeyDown", "mask", "autofix", "disableAutocompleteOnBlur", "showUnmaskedValue", "nativeInputAttributes"]);
    const baseProps = getBaseProps(rest);
    const formFieldContext = useFormFieldContext(rest);
    const inputRef = React.useRef(null);
    const [cursorPosition, setCursorPosition] = useState(null);
    useLayoutEffect(() => {
        var _a;
        if (cursorPosition !== null) {
            (_a = inputRef.current) === null || _a === void 0 ? void 0 : _a.setSelectionRange(cursorPosition, cursorPosition);
        }
    }, [cursorPosition, inputRef]);
    const _b = useMask({
        format: new MaskFormat(mask),
        value,
        inputRef,
        autofix,
        disableAutocompleteOnBlur,
        onChange: (value) => !rest.readOnly && fireNonCancelableEvent(onChange, { value }),
        onKeyDown: (event) => !rest.readOnly && onKeyDown && fireCancelableEvent(onKeyDown, event.detail, event),
        onBlur: () => fireNonCancelableEvent(onBlur),
        setPosition: setCursorPosition,
        showUnmaskedValue,
    }), { onPaste } = _b, maskProps = __rest(_b, ["onPaste"]);
    const inputProps = Object.assign(Object.assign(Object.assign(Object.assign(Object.assign({}, rest), baseProps), formFieldContext), maskProps), { value: showUnmaskedValue ? value : maskProps.value });
    const mergedRef = useMergeRefs(ref, inputRef);
    return (React.createElement(InternalInput, Object.assign({}, inputProps, { ref: mergedRef, nativeInputAttributes: processAttributes({
            onPaste,
        }, nativeInputAttributes, 'MaskedInput') })));
});
export default MaskedInput;
//# sourceMappingURL=index.js.map