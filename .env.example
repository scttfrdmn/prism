# CloudWorkstation Development Environment Configuration
# Copy this file to .env and modify as needed

# Development mode - avoids keychain password prompts during development
CLOUDWORKSTATION_DEV=true

# Test environment indicator (alternative to CLOUDWORKSTATION_DEV)
# GO_ENV=test

# Disable AWS credential chain for testing (uses mock/local credentials)
# AWS_PROFILE=test

# Debug logging level
# CLOUDWORKSTATION_LOG_LEVEL=debug